# DeepSeek Harness Base UI

An independently managed DeepSeek Harness plugin for reusable base UI capabilities.

The product scope, package name, settings surface, and Cordis composition will be defined before the first implementation. This repository intentionally contains no active plugin code yet.
